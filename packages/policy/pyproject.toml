[project]
name = "flowpilot-policy"
version = "0.1.0"
description = "FlowPilot deterministic PDP decision and obligation enforcement"
requires-python = ">=3.12"
dependencies = [
    "flowpilot-domain",
    "flowpilot-tool-contracts",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flowpilot_policy"]

[tool.uv.sources]
flowpilot-domain = { workspace = true }
flowpilot-tool-contracts = { workspace = true }
