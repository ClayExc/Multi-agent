[project]
name = "flowpilot-context"
version = "0.1.0"
description = "FlowPilot deterministic ContextEnvelope construction"
requires-python = ">=3.12"
dependencies = [
    "flowpilot-domain",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flowpilot_context"]

[tool.uv.sources]
flowpilot-domain = { workspace = true }
