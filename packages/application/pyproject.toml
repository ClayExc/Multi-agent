[project]
name = "flowpilot-application"
version = "0.1.0"
description = "FlowPilot application use cases and internal ports"
requires-python = ">=3.12"
dependencies = [
    "flowpilot-domain",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flowpilot_application"]

[tool.uv.sources]
flowpilot-domain = { workspace = true }
